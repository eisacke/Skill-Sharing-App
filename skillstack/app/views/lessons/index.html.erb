<main>
  <h2>All Lessons</h2>
  <div class="lesson_index_main_container">
    <div class="lesson_index_container">
      <%= render @lessons %>
    </div>
    <div class ="category_container">
      <% @categories.each do |category| %>
      <%= link_to category.name, category, class: "category_button" %>
      <% end %> 
    </div>
  </div>
</main>
