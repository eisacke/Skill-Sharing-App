<h1>Listing Lessons</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Image one</th>
      <th>Image two</th>
      <th>Image three</th>
      <th>Location</th>
      <th>Cost</th>
      <th>Teacher</th>
      <th>Category</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lessons.each do |lesson| %>
      <tr>
        <td><%= lesson.title %></td>
        <td><%= lesson.description %></td>
        <td><%= lesson.image_one %></td>
        <td><%= lesson.image_two %></td>
        <td><%= lesson.image_three %></td>
        <td><%= lesson.location %></td>
        <td><%= lesson.cost %></td>
        <td><%= lesson.teacher_id %></td>
        <td><%= lesson.category_id %></td>
        <td><%= link_to 'Show', lesson %></td>
        <td><%= link_to 'Edit', edit_lesson_path(lesson) %></td>
        <td><%= link_to 'Destroy', lesson, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lesson', new_lesson_path %>
